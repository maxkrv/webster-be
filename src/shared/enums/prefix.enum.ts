export enum Prefix {
  ROOT = '',
  ASSETS = 'assets',
  HEALTH = 'health',
  USERS = 'users',
  AUTH = 'auth',
  AUTH_GOOGLE = 'auth/google',
  PROJECTS = 'projects',
}

export const GLOBAL_PREFIX = 'api';
